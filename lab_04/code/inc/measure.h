#pragma once

#include "sorts.h"

void getTimeResults(size_t wordLenStart,
                    size_t wordLenStop,
                    size_t wordLenStep,
                    int countProcessed,
                    int threadCountStart,
                    int threadCountEnd);